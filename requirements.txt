# Pinned per §8.0: "Pinned requirements.txt; one command to set up.
# Version drift across five machines is a predictable time sink."
#
# Runtime (Index 2/6, §3.2/§3.4 Step 6)
numpy==1.26.4
scikit-learn==1.4.2
sentence-transformers==2.7.0
torch==2.2.2

# lightgbm was pinned here for the optional pairwise-objective comparison
# (§6.6 "Objective function", §8.3 step C6). C6 was closed as won't-do —
# explicitly time-permitting and off the critical path — so nothing in the
# codebase imports it. Removed: on macOS it fails to build without a
# separate `brew install libomp`, which broke `pip install -r
# requirements.txt` from a clean clone for an unused dependency.

# Analysis (§8.0: ablation tables, slice plots, feature distributions)
pandas==2.2.2
jupyter==1.0.0

# Dev tooling (§8.0)
pytest==8.2.0
ruff==0.4.4
black==24.4.2
